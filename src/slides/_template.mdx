export const meta = {
  title: 'Slide Title',
  reactions: ['🧡', '😎', '🤔'],
  background: '/path/to/background.jpg',
  // Opt-in per slide: show live reaction counters to presenter
  showLiveReactions: false,
  // Optional: sequence of audio transition clips (served from /public)
  // Each Next/Prev press plays the next/previous clip before changing slides
  // Example: ['/audio/intro-1.mp3', '/audio/intro-2.mp3']
  // audioTransitions: [],
  // Optional: matching hand command codes triggered with each audio/action fragment
  // Example: ['1', '3']
  // handActions: [],
};

# Big Idea Headline

## Optional subtitle line

- One concise supporting point
- Another concise point
- Keep bullets short

{/* This is a template. Files starting with '_' are ignored by the slide loader. */}
